# Future Capabilities

This directory records concepts that are intentionally outside the active ResearchFlow v1.2 runtime.

A concept belongs here until it has a real execution path, configuration, tests, and defined failure behavior. Runtime code under `backend/app/` should describe only what the deployed application actually uses.

See `frameworks-and-rag.md` for deferred framework adapters and retrieval/RAG ideas.
